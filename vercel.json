{
  "version": 2,
  "builds": [
    {
      "src": "payment-project/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/payment-project/dist/$1"
    }
  ]
}
